#!/bin/sh
set -eu

service_dir="$HOME/.config/rc.d"

for service in "$service_dir"/*; do
	short="$(basename "$service")"
	#echo "Stopping service \"$short\"..."
	"$service" stop
	#echo "Service \"$short\" stopped."
done
