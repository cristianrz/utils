#!/bin/sh

set -eu

cd "$(dirname "$0")"/..
ROOT="$PWD" export ROOT

. "$ROOT/etc/rc"

trap 'echo; . "$ROOT/etc/rc.shutdown"; exec "$ROOT/bin/profileinit"' 2
trap 'echo; . "$ROOT/etc/rc.shutdown"' 10

# unless we do this, it doesn't react to SIGUSR1
sleep infinity &
wait
